// Description
// Given an array of integers that is already sorted
// in ascending order of absolute value,
// find two numbers so that the sum of
// them equals a specific number.

// The function twoSum should return indices of
// the two numbers such that they add up to the target,
// where index1 must be less than index2.
// Note: the subscript of the array starts with 0

// You are not allowed to sort this array.

export class Solution {
  /**
   * @param nums: the input array
   * @param target: the target number
   * @return: return the target pair
   *          we will sort your return value in output
   */
  twoSumVII (nums, target) {
  }
}
